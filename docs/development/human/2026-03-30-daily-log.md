# 2026-03-30 开发日志

## 今日开发内容

1. **Dashboard.vue 卡片式布局重构**：
   - 参考 https://a8ab9068-26b0-4577-be71-1a232f036eed.dev.coze.site/ 完成样式重构。
   - 后端 `get_dashboard_signals` 接口更新为返回卡片所需数据结构，通过 TDD 测试验证。
   - 前端实现策略参数展示区、标签切换功能（全部基金、T+0、T+1）。
   - 每个基金卡片独立展示，突出显示预期利润（绿色）和最大亏损（红色）。
   - 前端构建通过，无 TypeScript 错误。

2. **Dashboard.vue 因 git restore 意外恢复为空壳后重写**：
   - 完整重写 659 行的卡片式布局页面。
   - 包含：顶部统计栏（4个stat-card）、策略参数区、标签切换（全部/T+0/T+1）、基金信号卡片网格。
   - 实现了 isDev mock fallback 机制（开发模式使用内联 mock 数据，生产模式调用 Tauri invoke）。
   - TypeScript 接口 `DashboardSignal` 严格对齐后端 15 个字段。

3. **全部 6 个页面实现 isDev mock fallback**：
   - 涉及页面：Dashboard.vue, FundList.vue, Screening.vue, Scoring.vue, Scheduler.vue, Config.vue。
   - 统一模式：`import.meta.env.DEV` 判断环境，DEV 模式使用内联 mock 数据，生产模式使用 `await import('@tauri-apps/api/core')` 动态导入。
   - 所有页面在 catch 分支也 fallback 到 mock 数据，确保容错。
   - 前端构建通过（`npm run build`），后端 37 个测试通过（2 个联网测试因无网络预期失败）。

## 验证结果

- `npm run build`：通过，无 TypeScript 错误，无 warning。
- `pytest src-python/tests/`：37 passed, 2 failed（联网测试，预期内）。

## 待完成任务

1. **前端 UI 确认与调整**：
   - Dashboard.vue 卡片布局样式需用户确认。
   - FundList.vue 改为卡片式布局。
   - 其他页面细节调整。

2. **实现全局色彩配置系统**：
   - 在 Config.vue 添加色彩偏好选择器（A股标准/国际标准）。
   - 实现全局颜色配置管理，支持动态切换。
   - 所有页面的盈亏显示都根据用户配置自动调整。

3. **集成 ECharts**：
   - 在 Scoring.vue 页面中集成 ECharts 并渲染基金的 K 线走势图。

4. **真实数据流接入**：
   - 替换后端 Python 提供的前端所需 Mock 数据，接入真正的数据库或 Akshare 实时抓取逻辑。

### UI 重构 Phase 1: 首页设计完全对齐 (Dashboard)

**完成情况:**
- 放弃之前的自作聪明响应式调整，完全接受并100%复制了用户提供的“瀑布流” HTML 设计稿。
- 引入 `Inter` 和 `JetBrains Mono` 字体，全面对齐原设计全局样式。
- 侧边栏 (`MainLayout.vue`) 还原为原设计的 DOM 结构与 class。
- 首页 (`Dashboard.vue`) 完全对齐，保留原始的瀑布流卡片结构、间距、对齐方式以及视觉元素。
- 把静态的 HTML 设计转化为动态的 Vue 3 组件，使用 TS 模拟数据（与原设计保持一致）进行渲染测试。

**今日总结 (22:00+ 收尾):**
- 彻底完成了首页和全局布局的基础框架与视觉重建，达到了用户期待的极简数据大屏效果。

### 明日开发计划 (2026-03-31)

1. **讨论与设计全量基金页 (FundList)**
   - 根据首页确定的极简、大留白、突出核心数据的风格，设计 `FundList.vue` 的交互和展示形式（表格、卡片列表或其他）。
   - 向用户呈现 Mock 数据支撑的 HTML/CSS 设计方案并等待确认。
2. **继续保持严格还原 UI 的原则**
   - 绝不随意改变间距或添加冗余的响应式结构，尊重设计图的美感。
3. **技术分析页与系统设置页的预研**
