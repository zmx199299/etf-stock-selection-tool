# 2026-04-04 开发日志

## 今日完成

- 复核并修正了第三页入口规则理解偏差，当前已按最终确认口径收口为“双页态”。
- 第三页 `Analysis.vue` 现在分为两种状态：
  - `/analysis` 无参数时，展示顶部横栏与基金卡片入口页。
  - `/analysis?code=...` 有参数时，直接进入纯详情页，不再显示顶部卡片区。
- 第三页入口卡片与首页现统一通过 `loadSharedFundCards()` 读取同源数据入口，满足“第三页每次打开先看第一页同源数据再生成”的规则。
- 第三页卡片点击与顶部搜索选择基金现统一走真实 `vue-router` 跳转，修复了真实浏览器里部分卡片点击无反应的问题。
- 已补齐第二页详情入口涉及但此前缺失的 `510500`、`588000` 轻量分析 Mock，保证从第二页跳转第三页时可直接进入详情态。
- 已补齐并更新相关测试，覆盖第三页双页态、真实路由跳转、同源数据加载器以及第二页详情入口基金的分析 mock 完整性。

## 本次验证

- `npm run test -- --run src/views/__tests__/Analysis.spec.ts src/views/__tests__/FundList.spec.ts src/views/__tests__/Dashboard.spec.ts src/utils/__tests__/analysisMock.spec.ts src/utils/__tests__/dashboardSignals.spec.ts`
- `npm run build`

## 当前状态

- 第三页入口规则已稳定：无参为卡片入口页，有参为纯详情页。
- 第三页与首页当前采用“同源数据即可”方案：共享同一个卡片数据加载入口，但不读取首页组件内部临时显示状态。
- 当前预览中第三页卡片点击、第一页点击进入、第二页详情分析进入都可稳定打开对应详情页。

---

## 第四页：系统设置页 (Settings) 完整实现

### 概述

设计并实现了第四页「系统设置」，路由 `/settings`。采用单列三卡片纵向堆叠布局，整合了分散在各页面的可配置项，并增加了应用信息与法律声明。

### 新增文件

- `src/stores/displaySettings.ts` — 新 Pinia store，管理卡片展示数量设置，支持 localStorage 持久化
- `src/stores/__tests__/displaySettings.spec.ts` — store 单元测试（6 个测试）
- `src/views/__tests__/Settings.spec.ts` — 设置页组件测试（13 个测试）

### 修改文件

- `src/views/Settings.vue` — 从空壳替换为完整三卡片设置页
- `src/views/Dashboard.vue` — 使用 `displaySettings.cardCount` 替代硬编码 10
- `src/views/Analysis.vue` — 传递 `displaySettings.cardCount` 给 `getAnalysisEntryCards()`
- `src/utils/dashboardSignals.ts` — `getAnalysisEntryCards()` 增加 `count` 参数
- `src/views/__tests__/Dashboard.spec.ts` — 新增 cardCount 集成测试
- `src/utils/__tests__/dashboardSignals.spec.ts` — 新增 count 参数测试
- `vite.config.ts` — 排除 `.worktrees/**` 避免 vitest 扫描旧测试文件

### 三卡片布局详情

1. **显示偏好**：红多/绿多切换（复用 `useColorModeStore()`，全局同步）；卡片展示数量下拉（6/8/10默认/12）
2. **关于**：应用名 FUNDFLOW、版本 v0.0.1 预览版、许可证 GPLv3、联网行为说明，2×2 网格布局
3. **隐私与免责声明**：三段文字，蓝/黄/红色圆点指示器

### 验证结果

- 全量测试 **112 个通过**（11 个测试文件），设置页前新增 21 个测试
- `vue-tsc --noEmit && vite build` 构建通过
- 用户浏览器确认页面无误

### 提交记录（6 个 commits）

1. `b33f4bd` — chore: 排除 .worktrees 目录避免 vitest 扫描旧测试
2. `a3f9afc` — feat: 新建 displaySettings Pinia store
3. `c718015` — feat: getAnalysisEntryCards 增加 count 参数
4. `32edb45` — feat: 实现系统设置页三卡片布局
5. `f2f9b59` — feat: Dashboard 页面从 displaySettings store 读取 cardCount
6. `48de520` — feat: Analysis 页面从 displaySettings store 读取 cardCount

---

## 下一步

1. 如需继续增强交互，可在 hover 浮层基础上补边界避让和更稳的定位策略。
2. 如需进入真实联调阶段，可优先把 `loadSharedFundCards()` 替换为稳定后端返回，并保持首页与第三页共用同一入口。
3. 在继续保持 Mock 优先和当前壳层不变的前提下，逐步收敛第三页轻量 analysis mock 的替换策略。
4. 前端四页已全部完成 Mock 阶段，可开始规划 Python 后端 JSON-RPC 服务对接。

---

## P0：真实数据基础链路完成与测试修复

### 概述

完成了 P0 阶段的后端真实数据基础链路收口，并对 Python 端失败测试做了系统化修复。当前 `get_fund_list` 与 `get_dashboard_signals` 已具备真实数据路径，FundList 也已具备 snake_case → camelCase 转换能力。

### 已完成内容

- 新增 `src-python/engine/sync.py`，实现 `DataSyncPipeline`
  - 支持基金列表同步
  - 支持日线行情同步
  - 支持净值同步
- 新增 `src-python/engine/services/fund_service.py`
  - 支持从数据库读取基金列表
  - 支持技术指标计算
  - 支持 FundList 所需的 MACD/RSI/BOLL/MA5/MA20 文字化输出
  - 支持评分映射到前端 1-10 分制
- 扩展 `src-python/engine/server.py`
  - 新增 `create_real_server()`
  - 新增基于数据库构建真实 dashboard signals 的路径
- 前端新增 `src/utils/fundList.ts`
  - 补齐 FundList 的 snake_case → camelCase 转换层
  - 统一真实返回与本地 mock 的加载口径

### 测试修复

本轮通过子代理模式修复了 4 个测试文件中的失败用例：

- `src-python/tests/test_sync.py`
  - 修正查询区间与断言数量不一致的问题（28 天而不是 30 天）
- `src-python/tests/test_fund_service.py`
  - 为第二只基金补齐有效夹具数据，恢复原测试意图
- `src-python/tests/test_server_real.py`
  - 在 fixture 中先同步 mock 数据，再创建真实 server
- `src-python/tests/test_data_source.py`
  - 对外部依赖型 akshare 集成测试增加环境鲁棒处理：外部空返回时 skip，非空时继续校验结构

### 本次验证

- `pytest src-python/tests/ -v`
  - 结果：`49 passed, 2 skipped, 1 warning`
- `npm run test -- --run`
  - 结果：前端 `112` 个测试全部通过
- `npm run build`
  - 结果：构建通过

### 当前状态

- P0 基础链路已经可用：同步、真实基金列表、真实 dashboard signals、FundList 转换层均已具备
- Python 端测试已恢复到可接受状态，外部源不稳定测试通过 skip 处理隔离环境影响
- 当前可以开始进入 P1：Analysis 页面真实接口设计与开发
