# 2026-04-04 开发日志

## 今日完成

- 复核并修正了第三页入口规则理解偏差，当前已按最终确认口径收口为“双页态”。
- 第三页 `Analysis.vue` 现在分为两种状态：
  - `/analysis` 无参数时，展示顶部横栏与基金卡片入口页。
  - `/analysis?code=...` 有参数时，直接进入纯详情页，不再显示顶部卡片区。
- 第三页入口卡片与首页现统一通过 `loadSharedFundCards()` 读取同源数据入口，满足“第三页每次打开先看第一页同源数据再生成”的规则。
- 第三页卡片点击与顶部搜索选择基金现统一走真实 `vue-router` 跳转，修复了真实浏览器里部分卡片点击无反应的问题。
- 已补齐第二页详情入口涉及但此前缺失的 `510500`、`588000` 轻量分析 Mock，保证从第二页跳转第三页时可直接进入详情态。
- 已补齐并更新相关测试，覆盖第三页双页态、真实路由跳转、同源数据加载器以及第二页详情入口基金的分析 mock 完整性。

## 本次验证

- `npm run test -- --run src/views/__tests__/Analysis.spec.ts src/views/__tests__/FundList.spec.ts src/views/__tests__/Dashboard.spec.ts src/utils/__tests__/analysisMock.spec.ts src/utils/__tests__/dashboardSignals.spec.ts`
- `npm run build`

## 当前状态

- 第三页入口规则已稳定：无参为卡片入口页，有参为纯详情页。
- 第三页与首页当前采用“同源数据即可”方案：共享同一个卡片数据加载入口，但不读取首页组件内部临时显示状态。
- 当前预览中第三页卡片点击、第一页点击进入、第二页详情分析进入都可稳定打开对应详情页。

## 下一步

1. 如需继续增强交互，可在 hover 浮层基础上补边界避让和更稳的定位策略。
2. 如需进入真实联调阶段，可优先把 `loadSharedFundCards()` 替换为稳定后端返回，并保持首页与第三页共用同一入口。
3. 在继续保持 Mock 优先和当前壳层不变的前提下，逐步收敛第三页轻量 analysis mock 的替换策略。
