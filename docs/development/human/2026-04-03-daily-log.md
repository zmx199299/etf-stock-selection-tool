# 2026-04-03 开发日志

## 今日完成

- 第三页 `Analysis.vue` 已从占位页改为研究报告主线型 Mock 页面，并统一到第二页 `FundList.vue` 的页面壳层节奏。
- 第三页已支持带基金代码直达与无代码时页内搜索/选择基金两种入口状态。
- 第三页顶部入口已改为承接第一页共享基金卡片数据，不再维护“两个固定示例基金”按钮；无 `code` 直达时顶部显示卡片、下方主体保持空态等待选择。
- 第三页顶部入口卡片已与首页共享数据源，并统一通过共享 getter 处理默认前 10 条、路由额外 `code` 插首与去重逻辑。
- 已为默认顶部入口涉及的共享基金补齐最小分析 Mock，点击顶部卡片后都能稳定进入分析态，不再回落为空态。
- 第三页规则已进一步收口为“双页态”：`/analysis` 无参数时显示顶部横栏 + 卡片入口页；`/analysis?code=...` 有参数时直接显示纯详情页，不再保留顶部卡片区。
- 第三页入口与首页现统一通过 `loadSharedFundCards()` 读取同源卡片数据；当前按你的确认采用“同源数据即可”规则，不额外耦合首页组件内部临时状态。
- 第三页卡片点击与顶部搜索选择基金现统一走真实 `vue-router` 跳转，不再仅修改本地地址状态，解决了“有的卡片点击无反应”的问题。
- 已补齐第二页详情入口涉及但此前缺失的 `510500`、`588000` 轻量分析 Mock，保证从第二页点击详情分析后可直接进入第三页详情态。
- 图表区已支持 `分时 / 日K / 5分 / 60分 / 120分 / 周K / 月K / 季K / 年K` 九种周期切换。
- 图表区已补齐价格轴、时间轴，并区分分时线图与 K 线两种 Mock 形态。
- 图旁解读、指标卡与全局 `红多 / 绿多` 配色方案已随当前周期联动。
- 已补齐所有非分时 K 周期的最小 `candles` Mock 数据，切换到 `m5 / m60 / m120 / week / month / quarter / year` 时不再出现空主图。
- 已修复长周期 K 线在 `quarter / year` 场景下可能溢出图表容器的问题，当前按活动周期价格范围缩放蜡烛高度，并约束实体高度不超过影线高度。
- 已修复季 K 时间轴因重复 `Q1` 文案触发 Vue `Duplicate keys found` 警告的问题，时间轴现使用稳定复合 key。
- 已补齐 `Analysis.spec.ts` 与 `analysisMock.spec.ts` 回归测试，覆盖非分时 K 周期实体渲染、长周期高度约束与季 K 重复 key 警告场景。
- 第三页图表区已补齐跟随鼠标的 hover 浮层：K 线可显示 `日期 / 开盘 / 收盘 / 最高 / 最低`，分时图可显示 `时间 / 价格 / 均价`，鼠标移出后浮层隐藏。

## 本次验证

- `npm run test -- --run src/utils/__tests__/analysisMock.spec.ts src/views/__tests__/Analysis.spec.ts src/views/__tests__/FundList.spec.ts src/views/__tests__/Dashboard.spec.ts`
- `npm run build`

## 当前状态

- 第三页技术分析页面的 Mock 主体已经稳定，当前样式可继续作为后续迭代基线。
- 第三页直接点击时已改为动态入口页：顶部承接第一页同源卡片数据，作为独立卡片入口页使用。
- 第三页带 `code` 进入时已切换为纯详情页，不再出现“卡片在上、详情在下”的混合态。
- 第三页图表已具备基础 hover 明细提示能力，当前可继续在不接真实图表库的前提下扩展更多交互细节。

## 今日开发代办清单

1. 在真实浏览器里复核第三页 hover 浮层在滚动、窄宽度和靠边位置下的定位与遮挡表现。
2. 为 `getAnalysisEntryCards()` 补一层 util 级行为测试，固定插首、去重和截断到 10 条的规则。
3. 评估第三页顶部入口、顶部搜索与真实基金列表/后端分析结果的联调切入点，明确轻量 mock 的替换顺序。

## 明日工作计划

1. 先做第三页 hover 浮层的浏览器实测，必要时补边界避让与更稳妥的定位约束。
2. 再给共享入口 card getter 增加独立测试，避免首页与第三页后续演进时出现逻辑漂移。
3. 最后整理第三页从 Mock 过渡到真实数据的接线方案，拆出下一轮最小联调任务。
