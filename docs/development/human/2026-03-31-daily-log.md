# 2026-03-31 开发日志

## 今日开发内容

1. **FundList 第二页 UI 方向确认**：
   - 基于浏览器可视化对比确认第二页采用 `A` 方案，即“首页风格外壳 + 宽表格主视图”。
   - 明确第二页不再使用首页那种瀑布流卡片，而是承担全量基金浏览与横向比较职责。
   - 确认列表采用整页长表滚动，不做分页。

2. **第二页关键交互确认**：
   - 每行保留 `详情分析` 按钮，整行仅做 hover 高亮，不做整行跳转。
   - 顶部保留 `红多 / 绿多` 切换器，并将其定义为全局配色入口。
   - 搜索框先只支持代码 / 名称的前端即时过滤。

3. **第二页视觉层级确认**：
   - 表格主焦点为 `涨跌幅`、`多空分值`、`详情分析` 按钮。
   - 技术指标列采用更克制的小号粗体文字，不做胶囊标签。
   - 继续保持白底、细边框、大圆角、轻阴影的统一视觉语言。

4. **设计文档输出**：
   - 新增设计文档：`docs/superpowers/specs/2026-03-31-fund-list-page-design.md`
   - 文档中同步明确了本轮实现范围、全局配色状态方案、响应式策略与验证边界。

## 当前状态

- 第二页设计已完成文字确认，等待用户审阅正式设计文档。
- 审阅通过后进入实现计划编写，再按 TDD 开始编码。

## 今日开发代办清单

1. 审阅并确认第二页设计文档：`docs/superpowers/specs/2026-03-31-fund-list-page-design.md`
2. 基于设计文档生成正式实现计划，明确测试、状态管理与页面改造顺序。
3. 按 TDD 启动第二页开发，优先落地全局配色状态与 `FundList.vue` 宽表格重构。

## 明日开发计划

1. 完成第二页设计文档审阅，锁定实现边界。
2. 编写第二页实现计划，拆分全局配色状态、`Dashboard.vue` 接入、`FundList.vue` 重构三个步骤。
3. 为前端补齐最小测试基线，覆盖全局配色状态与关键数据映射逻辑。
4. 开始实现 `FundList.vue` 新版宽表格界面，并接入全局 `红多 / 绿多` 配色切换。
5. 使用 `npm run build` 做前端构建验证，并同步更新开发文档。
