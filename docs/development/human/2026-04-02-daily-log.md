# 2026-04-02 开发日志

## 今日完成

- 以第二页 `FundList.vue` 为统一基准，收口首页 `Dashboard.vue` 的页面外层容器与顶部横栏宽度。
- 首页顶部横栏已改为与第二页一致的页面壳层节奏：`p-4 md:p-6` 外边距、白底圆角卡片式横栏、统一边框与阴影语义。
- 首页卡片信息口径已统一，移除了仅在单卡上出现的 `止损点` 字段，避免信息结构不一致。
- 明确后续页面统一规范：左侧导航栏不再调整；顶部横栏的风格与宽度统一；中间内容展示区作为功能页面重点修改区域。
- 通过真实像素测量确认：此前不一致并非外层宽度，而是顶部横栏内部结构不同；已将首页顶部栏重构为与第二页一致的三段框架。
- 补充开发约束：后续页面必须先搭建固定框架（左侧导航栏 / 顶部横栏 / 内容展示区），再在框架内部做页面功能和视觉调整，不能脱离框架单独发挥。
- 首页卡片区已从瀑布流列布局改为固定网格布局，确保卡片按行均布、两端对齐，不再出现整体左贴边的观感。

## 本次验证

- `npm run test -- --run src/views/__tests__/FundList.spec.ts src/views/__tests__/Dashboard.spec.ts`
- `npm run build`

## 当前状态

- 第二页 `FundList.vue` 仍是当前全项目的页面壳层基准。
- 第一页 `Dashboard.vue` 已按第二页基准完成顶部横栏结构统一、卡片区均布网格化，并去除多余的 `止损点` 展示。
- 下一步若继续调整首页，应聚焦中间内容展示区的排布与卡片细节，不再改左侧导航栏。

## 明日开发计划

1. 开始第三页“技术分析”页面设计与 Mock 预览。
2. 开工前先给出至少 3 种第三页页面预览方案，供用户比较后确认方向。
3. 在确认第三页方案前，不推进真实数据联动与复杂图表实现。
