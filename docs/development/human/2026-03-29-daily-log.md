# 2026-03-29 开发日志

## 今日开发内容总结

1. **Python 核心引擎开发**：
   - 完成了底层数据模型（SQLite 数据库）、数据源抽象（Akshare接入）。
   - 实现技术指标计算（MACD, RSI, BOLL 等）与形态识别框架。
   - 搭建了基于 `stdin/stdout` 通信的 JSON-RPC 服务端，并完成了 `ping`, `get_engine_status`, `fetch_legal_tax_rates` 等核心接口开发。
   - 采用 TDD 流程，34 个单元测试全部通过。

2. **Rust 中间层桥接**：
   - 编写了 `EngineManager` 进行 Python 子进程的管理。
   - 封装了 Tauri Commands (`start_engine`, `stop_engine`, `invoke_engine`)，负责前端请求的可靠转发。

3. **前端 Vue 3 UI 原型搭建与对接**：
    - 完成了包含导航栏、Dashboard、筛选页、打分页、配置页、定时任务页以及全量基金列表的代码结构。
    - **重大进展**：成功将原本写死在 Vue 内部的 Mock 数据，全部通过 `@tauri-apps/api/core` (`invoke`) 替换为调用后端 Python 接口的数据。前端与后端正式打通。
    - **卡片式布局重构**：将 `Dashboard.vue` 从传统表格布局改为现代卡片式布局，参考 https://a8ab9068-26b0-4577-be71-1a232f036eed.dev.coze.site/ 的设计风格。
      - 添加了顶部策略参数展示区（资金规模、目标收益、推荐日期）。
      - 实现了标签切换功能（全部基金、T+0、T+1）。
      - 每个基金卡片突出显示预期利润（绿色大号字体）和最大亏损（红色大号字体）。
      - 后端 `get_dashboard_signals` 接口已更新为返回卡片所需数据结构，并通过 TDD 测试验证。
      - 前端构建通过，无 TypeScript 错误。

## 当前未决事项（重要！）

**前端 UI 最终稿尚未确认**。
目前的页面仅为原型验证，以下内容均未经过用户最终审核：
- 各种表格的具体展示样式和字段分布。
- 各个页面的小细节与排版修正。
- 界面上现有的一些按钮（如某些页面的“操作"、"同步"等）的去留问题。

**新增重要需求**：页面中使用的色彩方案（红涨绿跌 vs 绿涨红跌）需要让用户可以自行配置。这对于适配不同市场习惯（A股 vs 国际市场）非常重要。

## 明日开发计划

1. **首要任务：前端原型确认与调整**。
   - 与用户逐个页面过一遍 UI 细节。
   - 调整表格、修正 UI 样式，确认所有按钮的去留与行为逻辑。
2. **实现全局色彩配置系统**：
   - 在 `Config.vue` 或单独的设置页面中添加色彩偏好选择器（A股标准/国际标准）。
   - 实现全局颜色配置管理，支持动态切换。
   - 所有页面的盈亏显示都根据用户配置自动调整。
3. **UI 风格重构**：
   - 参考 https://a8ab9068-26b0-4577-be71-1a232f036eed.dev.coze.site/
   - 将表格布局改为卡片式布局。
   - 突出显示盈亏信息。
4. **集成 ECharts**：
   - 待 UI 确认后，在 `Scoring.vue` 页面中集成 ECharts 并渲染基金的 K 线走势图。
5. **真实数据流接入**：
   - 替换后端 Python 提供的前端所需 Mock 数据，接入真正的数据库或 Akshare 实时抓取逻辑。