# ETF 智能分析系统 - AI 当前状态指针

**最后更新**: 2026-04-03 晚 (UTC+8)

## 1. 当前阶段

当前阶段为 **第三页技术分析页面 Mock 收口与交互补强节点**。

## 2. 当前已完成内容

- `FundList.vue` 宽表格页面已完成，包含搜索、空状态与详情入口。
- `FundList.vue` 已按验收反馈完成收口：顶部业务文案已改为“全量场内基金（不含货币/债券基金）/ 共监测X支”，大屏宽度已放开，代码/名称区域可跳转雪球详情页。
- 全局 `红多 / 绿多` 配色设置已完成，并接入本地持久化。
- `Dashboard.vue` 已接入共享配色，与全局设置保持联动。
- `Dashboard.vue` 已按第二页 `FundList.vue` 的固定框架统一顶部横栏结构，将卡片区改为均布网格布局，并移除了首页单卡独有的 `止损点` 展示。
- 前端测试基线已补齐，可通过 `npm run test -- --run` 进行回归验证。
- 第三页 `Analysis.vue` 已从占位页改为研究报告主线型 Mock 页面，并统一到第二页 `FundList.vue` 的页面壳层节奏。
- 第三页已支持带基金代码直达与无代码时页内搜索/选择基金两种入口状态。
- 第三页图表区已支持分时、日K、5分、60分、120分、周K、月K、季K、年K 九种周期切换。
- 第三页图表区已补齐价格轴、时间轴，并区分分时线图与 K 线两种 Mock 形态。
- 第三页图旁解读、指标卡和图表颜色已随当前周期及全局 `红多 / 绿多` 设置联动。
- 第三页所有非分时 K 周期现已补齐最小 `candles` Mock 数据，切换到 `m5 / m60 / m120 / week / month / quarter / year` 时可稳定渲染 K 线实体。
- 第三页已修复长周期 K 线高度溢出图表容器的问题，当前按活动周期价格范围缩放蜡烛高度，并约束实体不超过影线。
- 第三页已修复季 K 时间轴重复文案导致的 Vue 重复 key 警告，时间轴渲染现使用稳定复合 key。
- 当前前端相关回归测试与构建验证已通过：`47 passed`，`npm run build` 成功。

## 3. 当前约束

- 继续遵循 Mock 优先，不在用户确认前擅自推进真实数据联动或深度交互扩展。
- 第三页“技术分析”页面仍需以 Mock 结构和轻交互为主，在用户确认前不接真实图表库与真实后端数据。
- 后续页面统一遵循三区结构：左侧导航栏固定不改；顶部横栏风格与宽度统一；中间内容展示区作为功能页面重点修改区域。
- 后续页面的顶部横栏宽度与外层卡片结构统一以第二页 `FundList.vue` 为基准，不再各页单独发散。
- 页面开发流程补充约束：先搭建固定三段框架，再在框架内部做功能与视觉修改，不能跳过框架直接局部发挥。
- 第三页当前样式已获得正向反馈，后续迭代应保持现有页面壳层与视觉语言，重点修正入口逻辑和图表交互细节。

## 4. 下一步

- 修正第三页直接点击时的入口逻辑，不再默认呈现两个固定示例基金，而是优先承接第一页或路由带入的基金上下文。
- 为第三页图表补齐鼠标 hover 提示能力，至少让 K 线展示日期、开盘、收盘、最高、最低数据。
- 在继续保持 Mock 优先和现有样式不变的前提下，补强第三页交互完整性与阅读体验。
- 第二页 `FundList.vue` 作为当前最稳定的视觉基线，后续页面优先参照其横栏与容器结构。
