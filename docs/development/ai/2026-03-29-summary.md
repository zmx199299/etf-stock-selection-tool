# AI Context Summary: 2026-03-29

> 历史快照：本文记录的是 2026-03-29 的阶段性总结，**不是当前项目真实状态**。继续开发时请优先读取 `docs/development/ai/current_state.md`。

## Current State of the Project

The architecture is now fully connected:
`Vue 3 (Frontend)` <---> `Tauri Commands (Rust IPC)` <---> `JSON-RPC over stdio (Python)`

### Achievements
- Python JSON-RPC server handles parallel and sequential method calls successfully.
- Frontend Vue components have been stripped of hardcoded mock data. They now fetch data via `invoke('invoke_engine', { method: 'xxx' })`.
- Python methods implemented: `fetch_legal_tax_rates`, `get_fund_list`, `get_dashboard_signals`, `get_screening_results`, `get_scoring_data`, `get_scheduler_data`. All with corresponding passing Pytest units.
- **Dashboard.vue 重构为卡片式布局**：已根据参考页面 https://a8ab9068-26b0-4577-be71-1a232f036eed.dev.coze.site/ 完成样式重构。
  - 后端 `get_dashboard_signals` 接口更新为返回卡片所需数据结构，通过 TDD 测试验证。
  - 前端实现策略参数展示区、标签切换功能、卡片式基金展示。
  - 预期利润和最大亏损使用国际色彩标准（绿涨红跌）高亮显示。

### Crucial Directives for Next AI Session
1. **STOP AND ASK FOR UI CONFIRMATION**: Do not proceed with adding new logic or ECharts *until* you prompt the user to review the current frontend UI. The user stated explicitly: "今天我们的前端还没完成确认...包括各种表格、各种页面的小修正、还有一些按键是否保留都没确认". 
2. Wait for the user's specific feedback on tables, buttons, and layouts before touching the `src/views/` styling or structure.
3. Obey the `workflow_rules.md` (Skills) on subsequent interactions (cold backups and logging).
