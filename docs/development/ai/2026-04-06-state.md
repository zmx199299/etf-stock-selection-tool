# 2026-04-06 Project State

**当前版本**: v0.0.13
**最后更新**: 2026-04-06 (Late Night)

## 重大进展

### v0.0.8 — Python 引擎启动崩溃修复
- 修复 `AkshareDataSource` → `AkshareSource` import 错误
- 添加缺失的 `db.init()` 调用
- 4 个新测试

### v0.0.9 — 五重启动同步修复
1. `startupSync.ts`: `sync_data` → `ping` 避免全量同步超时
2. `engine.rs`: BufReader 持久化避免数据丢失
3. `main.ts`: `start_engine` 失败正确传播错误状态
4. `main.py`: 后台同步使用独立数据库连接避免 SQLite 线程冲突
5. `engine.rs`: 生产环境 stderr 重定向到 `~/.etf-analyzer/engine.log`

### v0.0.10 — 生产模式 sidecar 路径修复
- `BaseDirectory::Resource` 在 macOS/Linux 解析到错误路径
- 改用 `std::env::current_exe().parent()` 匹配 tauri-plugin-shell 逻辑

### v0.0.11 — 多源容错数据爬取
- **问题**: 东方财富 (`_em`) API 全面 ConnectionError，数据同步完全失败
- **解决**: 多源并发 + 自动降级
  - `fetch_fund_list()`: ThreadPoolExecutor 并发请求 em/sina，取数据更多的
  - `fetch_daily_quotes()`: em 优先，失败降级到 sina
  - 新浪源交叉引用 `fund_name_em()` 元数据，过滤货币/固收/债券

### v0.0.12 — PyInstaller 打包修复
- `--paths=src-python` 修复 `ModuleNotFoundError: No module named 'engine'`
- `--collect-data=akshare` 打包 akshare 内部数据文件

### v0.0.13 — Windows 三重修复（当前版本）
- **Fix 1**: Windows `--noconsole` 消除 engine.exe 终端窗口
- **Fix 2**: `--collect-all=py_mini_racer` 打包 sina 日线依赖的 native DLL
- **Fix 3**: `sync_daily_quotes_for_all()` 连续失败早期终止（默认 20 只后中断）

## 当前测试结果
- Python: 104 passed
- Frontend: 86 passed (15 test files)
- Build: passing

## 关键文件变更
| 文件 | 变更 |
|------|------|
| `.github/workflows/release.yml` | --noconsole + --collect-all=py_mini_racer |
| `src-python/engine/sync.py` | max_consecutive_failures 早期终止 |
| `src-python/tests/test_sync.py` | 3 个早期终止测试 |

## 下一步
- 推送 v0.0.13 tag 触发 CI 三平台构建
- 用户在 Windows 上测试打包应用
- 继续推进业务功能（分钟线按需实时获取等）
