# 2026-04-06 Project State

**Major Accomplishments**:
1. Successfully implemented Scoring view with real data (`get_scoring_data` via JSON-RPC).
2. Cleaned out all Vue mock data, replacing with proper Empty States / error captures.
3. ECharts is fully integrated, replacing the fake SVG canvas in Analysis view.
4. Tauri backend sidecar (Python engine) connects directly to SQLite database inside `~/.etf-analyzer`.
5. Github Actions properly bundles `--hidden-import` dependencies using PyInstaller for Linux, MacOS (x86 & arm64) and Windows.

**Critical Bug Fix (Night Session)**:
- **Root Cause**: `main.py` had two fatal bugs preventing engine startup in production:
  1. Wrong import name: `AkshareDataSource` → should be `AkshareSource`
  2. Missing `db.init()` call after `Database(db_path)` — `self.conn` was `None`
- **Fix**: Corrected import name, added `db.init()`, added 4 startup tests
- **Version**: Unified all config files to `0.0.8`

**Current Test Results**:
- Python: 90 passed, 2 skipped
- Frontend: 84 passed
- Build: passing

**Next Steps**:
- Commit and push all changes to `origin/main`.
- Create Git Tag `v0.0.8` to trigger GitHub Actions release.
- Download and verify production app launches successfully on all platforms.
- If startup sync still fails, investigate Tauri sidecar process management.
